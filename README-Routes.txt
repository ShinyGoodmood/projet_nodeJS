Routes:

/test -> affiche "Hello"

/login(username&password) -> connexion

/register(username&password) -> création d'un username

/profil/(id) -> Affiche le profil de l'utilisateur avec l'id correspondant

/user/rm/(id) -> supprime l'utilisateur avec l'id correspondant

/user/up/(id) -> Passe en admin l'utilisateur avec l'id correspondant

/user/down/(id) -> retire le rôle admin de l'utilisateur avec l'id correspondant

/user/ban/(id) -> Bannit l'utilisateur avec l'id correspondant